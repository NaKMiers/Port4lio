# skill-sync — memory

Self-improving notes for the project-local Codex↔Claude sync. Append lessons here;
this is the single canonical copy (no Claude-side duplicate).

## Model

- Canonical: `.agents/skills/<name>/` (SKILL.md + references/ + agents/openai.yaml).
- Claude wrappers: `.claude/skills/<name>/SKILL.md` (thin, delegating).
- Root: `AGENTS.md` canonical; `CLAUDE.md` = `@AGENTS.md`.
- Exemptions (never touch): `.agents/rules/`, `.agents/_shared/`, `projects/`,
  `.gstack/`, vendored `browse`, binary assets.

## Lessons

- (none yet)
